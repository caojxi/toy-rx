import assert from 'assert'
import Rx from './../js/rx/index.js'

describe('Observable', () => {
  it('should support the example use case', done => {
    console.log(Rx.Observable)

    done()
  })
})